name: Smoke Test

on:
    push:
    workflow_dispatch:

jobs:
    smoke:
        runs-on: ubuntu-latest

        steps:
            - name: Say hello
              run: echo "GitHub Actions is working"
